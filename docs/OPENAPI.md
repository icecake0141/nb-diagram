# OpenAPI

## English

- Contract file: `docs/openapi.yaml`
- Runtime endpoint: `GET /api/openapi.yaml`

Use the YAML file as the source of truth for API client generation and schema validation.

---

## 日本語訳

- 契約ファイル: `docs/openapi.yaml`
- 実行時エンドポイント: `GET /api/openapi.yaml`

API クライアント生成やスキーマ検証では、この YAML を正本として扱ってください。
